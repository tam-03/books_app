<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @report.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @report.body %>
</p>

<% if current_user == @report.user %>
<%= link_to 'Edit', edit_report_path(@report) %> |
<% end %>
<%= link_to 'Back', reports_path %> |
<%= link_to 'コメント一覧', report_comments_path(@report) %> |
<%= link_to 'コメントする', new_report_comment_path(@report) %>
