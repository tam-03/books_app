<h1><%= t('.follows_page') %></h1>

<% @users.each do |user| %>
  <hr>
  <p><strong><%= link_to user.name, user_path(user.id) %></strong></p>
  <p><%=link_to t('.followed'), user_user_relationships_path(user.id), method: :delete %></p>
<% end %>