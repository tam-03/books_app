
<h1><%= t('.title') %></h1>

<table>
  <thead>
    <tr>
      <th><%= Comment.human_attribute_name(:body) %></th>
      <th><%= Comment.human_attribute_name(:user) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.body %></td>
        <td><%= comment.user.name %></td>
        <% if current_user == comment.user %>
        <td><%= link_to t('.show'), [@book, comment] %></td>
        <td><%= link_to t('.edit'), [:edit, @book, comment] %></td>
        <td><%= link_to t('.destroy'), [@book, comment], method: :delete, data: { confirm: t('.confirm') } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('.new_comment'), new_book_comment_path %>

<%= paginate @comments %>
